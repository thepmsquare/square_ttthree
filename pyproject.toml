[project]
name = "square-ttthree"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tqdm>=4.68.4",
]

[dependency-groups]
dev = [
    "ruff>=0.15.21",
]
